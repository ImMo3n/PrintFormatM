{
  "name": "PrintFormatM",
  "version": "0.5.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "backend": "json-server --watch db.json --port 5000",
    "preview": "vite preview --port 5050"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "vue": "^3.2.31"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.2.2",
    "vite": "^2.8.4"
  }
}
